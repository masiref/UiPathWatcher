<div class="tile is-parent clients">
    <article class="tile is-child box notification is-purple">
        <p class="title">{{ $clientsCount }}</p>
        <p class="subtitle">
            <span class="icon"><i class="fas fa-building"></i></span>
            <span>Customer{{ $clientsCount > 1 ? 's' : ''}}</span>
        </p>
    </article>
</div>