<div class="tile is-parent clients">
    <article class="tile is-child box">
        <p class="title">{{ $clientsCount }}</p>
        <p class="subtitle">Client{{ $clientsCount > 1 ? 's' : ''}}</p>
    </article>
</div>