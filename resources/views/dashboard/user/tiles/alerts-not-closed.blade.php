<div class="tile is-parent alerts-not-closed">
    <article class="tile is-child box">
        <p class="title">{{ $openedAlertsCount }}</p>
        <p class="subtitle">Pending alert{{ $openedAlertsCount > 1 ? 's' : ''}}</p>
    </article>
</div>